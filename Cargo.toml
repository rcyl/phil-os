[package]
name = "phil-os"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
# Basic BIOS bootloeader without C dependencies, just Rust and inline assembly
bootloader = "0.9"